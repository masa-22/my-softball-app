/**
 * 1球ごとのコース入力UIコンポーネント。
 * - ストライクゾーン上でコースをクリック/タップして入力する。
 * - 投球結果（ストライク/ボール/ファウル/ヒット等）も入力できるようにする。
 * - 現在の打者・投手情報も併せて表示する。
 */
import React from 'react';

const PitchCourseInput: React.FC = () => {
  // ここにコース入力UIを後で実装
  return <div>コース入力UI（後で実装）</div>;
};

export default PitchCourseInput;
