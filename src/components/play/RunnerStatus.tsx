/**
 * ランナー状況表示用コンポーネント。
 * - 1塁/2塁/3塁のランナー有無や選手名を表示する。
 * - 進塁・アウトなどの操作もここで行う予定。
 */
import React from 'react';

const RunnerStatus: React.FC = () => {
  // ここにランナー状況UIを後で実装
  return <div>ランナー状況（後で実装）</div>;
};

export default RunnerStatus;
